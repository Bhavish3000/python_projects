""" This module calculates the area of triangle
"""

height = int(input("Enter the height:")) # Take's input (height) as an interger
base = int(input("Enter the base:")) # Take's input (base) as an integer
area = 0.5*height*base
print(f"Area of the Triangle : {area}")
